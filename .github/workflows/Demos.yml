name: Demo Workflow

on:
  push:
    branches:
      - main

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Echo Hello World
        run: echo "Git Hub Actions working!"

      - name: list current directory
        run: ls
